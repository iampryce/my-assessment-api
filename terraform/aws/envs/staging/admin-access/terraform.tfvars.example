# Copy to terraform.tfvars (gitignored) and fill in your own IAM identity.
# Apply this root locally only - never wire it into a CI workflow. See
# backend.tf for why.

# admin_principal_arns = ["arn:aws:iam::<account-id>:user/<your-iam-user>"]
